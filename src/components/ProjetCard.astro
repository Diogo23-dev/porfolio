---
const {title, description, technologies, image} = Astro.props;
---


    <div class="project-card">
        <img src={image} alt={title}>
        <div class="project-content">
            <h3>{title}</h3>
            <p>
            {description}
            </p>

            <div class="technologies">
                {technologies.map((tech) => (
                    <span>{tech}</span>
                ))}
            </div>

            <div class="project-links">
                <a href="#">
                    Voir le projet
                </a>

                <a href="#">
                    GitHub
                </a>
            </div>

        </div>

    </div>
